import {StyleSheet} from 'react-native';
import {FONTS, COLORS} from '../../../../exporter';


const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: COLORS.background,
        alignItems: 'center',
    },
});

export default styles;
